# Datasource provisioning — local Prometheus inside otel-lgtm only.
# No external exporters.
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://localhost:9090
    isDefault: true
    editable: false
