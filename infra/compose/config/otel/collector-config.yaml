# otel-lgtm ships an integrated collector/receiver stack with defaults.
# Memdot does not override collector configuration in Phase 2.1.
# Custom exporter wiring arrives in Phase 2.2/2.3 when telemetry modes are typed.
